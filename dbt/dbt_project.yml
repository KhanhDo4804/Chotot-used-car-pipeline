name: chotot_pipeline
version: '1.0'
config-version: 2

profile: chotot_snowflake
model-paths: ["models"]
test-paths: ["tests"]
snapshot-paths: ["snapshot"]

models:
  chotot_pipeline:
    staging:
      +schema: staging
      +materialized: view
    intermediate:
      +schema: intermediate
      +materialized: table
    mart:
      +schema: marts
      +materialized: table
